// Include tileset_ref: { cdn_base_url, format: ("svg-slices"|"vector-tiles"), version, files:{...}, byte_size_total, slice_count|tile_count }.
// Rule: once referenced by a Performance, the entire seatmap doc and tileset_ref are immutable.
