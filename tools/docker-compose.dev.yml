# Dev stack (spec)
# Services:
# - Postgres: port 5432, volume pgdata; healthcheck: pg_isready
# - Redis: port 6379; healthcheck: redis-cli ping
# - Mongo: port 27017, volume mongodata; healthcheck: mongosh --eval 'db.runCommand({ ping: 1 })'
# Env: sourced from .env (local), never commit secrets; compose reads environment: []
